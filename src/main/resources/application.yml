spring:
  datasource:
    driver-class-name: org.h2.Driver
    password: sa
    url: jdbc:h2:mem:testdb
    username: sa
  jpa:
    properties:
      hibernate:
      show_sql: true
    mail:
      host: smtp.gmail.com
      port: 587
      username: email
        password:email_pwd
properties:
mail:
smtp:
starttls:
enable: true
auth: true					